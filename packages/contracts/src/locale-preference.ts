// Duplicated here rather than imported from `@ai-visibility/i18n` to avoid a
// `packages/contracts` -> `packages/i18n` dependency for a single 3-value union.
export type SupportedLocale = 'en' | 'es' | 'pt-BR';
